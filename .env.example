# farmOS Environment Variables
#
# For git worktree support, copy this file to ~/.config/farmOS/.env
# This allows the same secrets to work across all worktrees.
#
# Quick setup:
#   mkdir -p ~/.config/farmOS
#   cp .env.example ~/.config/farmOS/.env
#   # Edit ~/.config/farmOS/.env with your actual keys

# Mapbox token for address search and map features
# Get your token at https://account.mapbox.com/access-tokens/
VITE_MAPBOX_TOKEN=your_mapbox_token_here

# OpenAI API key for chat features (optional)
# Get your key at https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_key_here
